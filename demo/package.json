{
  "name": "modern-router-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"esbuild app.js --bundle --loader:.js=jsx --format=esm --external:react --external:react-dom --external:react-dom/client --outfile=dist/bundle.js --sourcemap --watch\" \"serve .\"",
    "build": "esbuild app.js --bundle --loader:.js=jsx --format=esm --external:react --external:react-dom --external:react-dom/client --outfile=dist/bundle.js --minify",
    "preview": "serve ."
  },
  "dependencies": {
    "history": "^5.3.0",
    "nprogress": "^0.2.0",
    "path-to-regexp": "^6.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "serve": "^14.2.3",
    "concurrently": "^8.2.2"
  }
}
